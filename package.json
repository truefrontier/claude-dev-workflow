{
  "name": "@truefrontier/claude-dev-workflow",
  "version": "2.0.0-beta.6",
  "description": "An automated software development pipeline that uses Claude AI to guide issues through structured development stages with human oversight.",
  "keywords": [
    "github",
    "workflow",
    "automation",
    "claude",
    "ai",
    "development",
    "pipeline",
    "bdd",
    "testing",
    "cli"
  ],
  "author": "True Frontier",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/truefrontier/claude-dev-workflow.git"
  },
  "bugs": {
    "url": "https://github.com/truefrontier/claude-dev-workflow/issues"
  },
  "homepage": "https://github.com/truefrontier/claude-dev-workflow#readme",
  "bin": {
    "claude-dev-workflow": "bin/cli.js"
  },
  "preferGlobal": false,
  "scripts": {
    "setup:labels": "node -e \"const os = require('os'); const { execSync } = require('child_process'); const script = os.platform() === 'win32' ? 'scripts/setup-labels.bat' : './scripts/setup-labels.sh'; try { execSync(script, { stdio: 'inherit' }); } catch (error) { console.error('Setup failed:', error.message); process.exit(1); }\"",
    "setup:help": "node scripts/setup-help.js",
    "validate:workflows": "node scripts/validate-workflows.js",
    "docs:labels": "node -e \"console.log('\\nWorkflow Labels:\\n=============\\n\\n🔵 needs:* (AI working):\\n- needs:triage, needs:triage-revision\\n- needs:spec, needs:spec-revision\\n- needs:architect, needs:architect-revision\\n- needs:develop, needs:develop-revision\\n\\n🟢 review:* (human review):\\n- review:triage, review:spec\\n- review:architect, review:develop\\n\\n🔴 error:* (needs help):\\n- error:triage, error:spec\\n- error:architect, error:develop\\n\\nUsage: Only one workflow label should be active per issue at a time\\n');\"",
    "postinstall": "echo '\\n🚀 Claude Dev Workflow installed!\\n\\nNext steps:\\n1. Run: npx @truefrontier/claude-dev-workflow help\\n2. Initialize: npx @truefrontier/claude-dev-workflow init\\n'"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^9.4.1",
    "fs-extra": "^10.1.0",
    "inquirer": "^8.2.6",
    "js-yaml": "^4.1.0",
    "open": "^8.4.2",
    "ora": "^5.4.1"
  },
  "files": [
    "bin/",
    "lib/",
    "templates/",
    "workflows/",
    "scripts/",
    "CLAUDE.md",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
